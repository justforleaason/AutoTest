// math_operations_params.test.jsconst mathOperations = require('./math_operations');// Параметризовані тести для функції add()test.each([    [3, 5, 8],    [-3, 5, 2],    [0, 0, 0],])('Додавання %i та %i повинно бути рівним %i', (a, b, expected) => {    expect(mathOperations.add(a, b)).toBe(expected);});// Параметризовані тести для функції subtract()test.each([    [10, 7, 3],    [10, -3, 13],    [0, 0, 0],])('Віднімання %i з %i повинно дорівнювати %i', (a, b, expected) => {    expect(mathOperations.subtract(a, b)).toBe(expected);});// Параметризовані тести для функції multiply()test.each([    [4, 3, 12],    [-4, 3, -12],    [0, 10, 0],])('Множення %i на %i повинно дорівнювати %i', (a, b, expected) => {    expect(mathOperations.multiply(a, b)).toBe(expected);});// Параметризовані тести для функції divide()test.each([    [10, 0, 'Помилка: ділення на нуль!']  // Updated to the correct Ukrainian phrase for "Error: division by zero!"])('division of %i by %i should result in %s', (a, b, expected) => {    expect(mathOperations.divide(a, b)).toBe(expected);});